# run this command to create the environment:
# conda env create --file fuzzycat_env.yml

name: fuzzycat-env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - astrolink
  - matplotlib
  - numpy
  - scikit-learn
  - pip
  - pip:
      - fuzzycategories